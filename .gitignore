# Rust
target/

# macOS
.DS_Store

# App iOS : repo séparé (sunmap-ios)
ios/

# Site web : repo séparé (sunmap-web)
web/

# Config MCP locale : contient un secret d'intégration Notion, jamais commit.
.mcp.json

# Configuration locale et secrets — `helios-server/.env` (base de données,
# identifiants OAuth OpenStreetMap) et le `.env` de racine que compose lit pour
# le tag de l'image. Le motif sans barre oblique couvre les deux.
.env

# Archives PMTiles générées par scripts/build-pmtiles.py — lourdes, régénérables.
tiles/

# PBF téléchargés et extraits intermédiaires du pipeline import-zone.sh.
pbf/
.venv-tiles/

# Python
__pycache__/
